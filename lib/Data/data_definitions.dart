// this file contains all classes we need to define data

// class for images we want to have a circle border
class CircleImageData {
  double size; // size will be used as height and width of image
  String image;
  // define image variable later on when doing backend
  CircleImageData({this.size, this.image});
}
