import 'package:flutter/material.dart';

// widget for user's public post history in profile page
class MyPublicHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text("public post history");
  }
}
